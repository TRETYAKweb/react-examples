/*
    Запустить команду — `tsc --build ./3`
    указать свойству lib значение — ["DOM"] или []
    это приведёт к тому что файл успешно скомпилируется в первом варианте
    и не сможет скомпилироваться во втором
*/

const el = document.getElementById('greeting');
