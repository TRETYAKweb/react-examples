/*
    Запустить команду — `tsc --build ./11`
    указать свойству composite значение — true или false
    это свойство накладывает некоторые ограничения на этапе сборки файлов и
    помогает сборщикам определить собирался ли уже проект
*/

export const sum = (a: number, b: number): number => a + b;

