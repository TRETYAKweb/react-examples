/*
    Запустить команду — `tsc --build ./10`
    указать свойству rootDir значение — ./src
    это свойство определяет директорию исходных файлов
*/

export const sum = (a: number, b: number): number => a + b;

