/*
    Запустить команду — `tsc --build ./8`
    указать свойству sourceMap значение — true или false
    это свойство определяет необходимость генераци .js.map файлов
*/

const sum = (a: number, b: number): number => a + b;
