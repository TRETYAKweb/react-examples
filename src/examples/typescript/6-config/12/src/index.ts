/*
    Запустить команду — `tsc --build ./12`
    указать свойству removeComments значение — true или false
    это свойство опередояют попадут ли комментарии в результирующий файл
*/

export const sum = (a: number, b: number): number => a + b;

